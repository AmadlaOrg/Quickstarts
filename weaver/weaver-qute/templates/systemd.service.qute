[Unit]
Description={description}
After=network.target

[Service]
Type=simple
User={user}
Group={group}
ExecStart={exec_start}
Restart={restart}
RestartSec={restart_sec}
{#for entry in environment.entrySet}
Environment={entry.key}={entry.value}
{/for}

[Install]
WantedBy=multi-user.target
