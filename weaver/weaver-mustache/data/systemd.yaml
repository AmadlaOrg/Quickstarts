service_name: myapp
description: My Application Service
exec_start: /usr/local/bin/myapp serve
user: www-data
group: www-data
restart: always
restart_sec: 5
environment:
  - key: LOG_LEVEL
    value: info
  - key: NODE_ENV
    value: production
  - key: PORT
    value: "3000"
